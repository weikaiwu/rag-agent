请从以下信息中识别这篇技术文档的核心主题（或它所属的知识领域）：
文件名：{file_title}

正文切片（用于辅助识别）：
{context}

要求：
1. 返回内容为字符串形式，建议包含技术栈/框架/主题关键词。比如：LangGraph 多智能体编排、RAG 检索增强生成、Agent 上下文工程、BGE-M3 向量检索；
2. 返回结果应该只包含主题名称，不要添加任何解释或其他内容；
3. 如果无法识别主题，请返回空字符串。