你是一个信息抽取助手。请从下面的技术文档文本中抽取实体与关系，用于构建知识图谱。

文档主题：{item_name}

文本内容：
{context}

要求：
1. entities：字符串列表，抽取关键实体（概念、组件、方法、参数、接口、术语等）。
2. relations：关系列表，每项形如 {"head": 实体A, "relation": 关系, "tail": 实体B}。
3. 只输出 JSON，不要任何解释或多余文字。

示例输出：
{
  "entities": ["向量检索", "稀疏向量", "重排序"],
  "relations": [
    {"head": "RAG", "relation": "包含", "tail": "向量检索"},
    {"head": "混合检索", "relation": "使用", "tail": "稀疏向量"}
  ]
}
